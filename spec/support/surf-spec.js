import { Surf } from './Surf.js';

describe("Surf", function() {
  let newSurf;
  

  beforeEach(function() {
    newSurf = new Surf(3,3,3,3);
  
  });

  it ( "determines the age of a person on mercury and also the life expectancy", function() {

  expect(newGalaxy.calculateScore()).toEqual(4)
});
})